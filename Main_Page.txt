// =============================
// MEDICARE+ MAIN PAGE
// =============================

document.addEventListener("DOMContentLoaded", () => {

    // Doctor Portal
    const doctorBtn = document.querySelector(".doctor")
        .parentElement.querySelector("a");

    doctorBtn.addEventListener("click", function(e){

        e.preventDefault();

        window.location.href = "Doctor_Login.html";

    });

    // Patient Portal
    const patientBtn = document.querySelector(".patient")
        .parentElement.querySelector("a");

    patientBtn.addEventListener("click", function(e){

        e.preventDefault();

        window.location.href = "Patient_Login.html";

    });

    // Receptionist Portal
    const receptionistBtn = document.querySelector(".receptionist")
        .parentElement.querySelector("a");

    receptionistBtn.addEventListener("click", function(e){

        e.preventDefault();

        window.location.href = "Receptionist_Login.html";

    });

    // Admin Portal
    const adminBtn = document.querySelector(".admin")
        .parentElement.querySelector("a");

    adminBtn.addEventListener("click", function(e){

        e.preventDefault();

        window.location.href = "Admin_Login.html";

    });

});